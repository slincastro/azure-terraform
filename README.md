# azure-terraform

> Install Azure CLI :

`brew update && brew install azure-cli`

`az login`

> For macOS :

`brew install terraform`

> Start terraform : 

`terraform init`

`terraform plan -out test.tfplan`